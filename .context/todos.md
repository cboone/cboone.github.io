# Todos

## Security

- [ ] Consider replacing innerHTML with DOM API in `assets/js/fastsearch.js` to mitigate theoretical XSS risk (search results are built from local JSON index, risk is minimal but DOM API would be safer)
